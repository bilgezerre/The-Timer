# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'The Timer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'RealmSwift', '~>10'
  pod 'SDWebImage', '~> 5.11.1'
  pod 'SwiftMessages'
  pod 'Charts'
  pod 'FittedSheets'
  
  # Pods for The Timer

  target 'The TimerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'The TimerUITests' do
    # Pods for testing
  end

end
